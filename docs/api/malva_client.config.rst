Configuration
=============

The :class:`~malva_client.config.Config` class manages client settings
(server URL, API token, SSL verification).  Configuration is read from
``~/.malva/config.json`` and can be overridden by environment variables.

.. autoclass:: malva_client.config.Config
   :members:
   :no-special-members:
